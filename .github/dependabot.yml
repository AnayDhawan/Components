version: 2
updates:
  # Keep the validation workflow's actions current. No package manifest ships
  # with this skill, so github-actions is the only ecosystem to track.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    labels:
      - "dependencies"
      - "ci"
